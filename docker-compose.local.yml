services:
  app:
   build:
    context: .
   container_name: notes-app-local
   restart: unless-stopped
   environment:
     PORT: 3000
     NODE_ENV: development
   ports:
     - "3000:3000"
   volumes:
     - .:/app
     - /app/node_modules
